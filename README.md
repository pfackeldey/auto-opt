# auto-opt

The `SKILL.md` is a template for an agentic skill to perform automatic (Python) code optimization in an iterative loop.
A concrete example is given in `AGC.md` which replaced the placeholders of the template (`[FILL THIS IN]`) with concrete context for the [Analysis Grand Challenge](https://github.com/iris-hep/analysis-grand-challenge).
